# Helps fresh site.json edits show without stale CDN/server cache (Apache + mod_headers).
<IfModule mod_headers.c>
  <FilesMatch "^(site\.json)$">
    Header set Cache-Control "no-store, no-cache, must-revalidate, max-age=0"
  </FilesMatch>
</IfModule>
